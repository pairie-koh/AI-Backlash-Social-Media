Every AI company just got caught lying. UC Berkeley just released a study proving how basically every AI benchmark is essentially just horse shit. The actual model wrote a test that just returned a successful test, regardless of what the code looked like. So it was just showing a successful test pass, even if the code didn't work. If the actual test sets contained the answers in the question, so the model just accessed the answer and then gave the correct answer. The benchmarks, the actual benchmark just checked if the model answered ever. So even just an empty response would result in a successful benchmark pass. benchmarks use llms as a judge to judge whether the lm actually passed the benchmark they're vulnerable to prompt injection and so on and a lot of the time they were just not giving the correct answer a lot of training data contamination so something like swe bench will contain a long list of programming problems that an ai agent has to go and solve but if you go just go and train the model on the questions then it goes and solves the questions a lot better this is a great example of something called Goodhart's Law. When a measure becomes the target, the measure stops mattering. People get to go and decide what model should I use by just looking at benchmarks, as many people did. Why not just go and optimize for higher benchmark scores? That's exactly what a lot of these labs did.