That's how we go to you. Got it. What is a hash table and why is it useful? Yeah, so a hash table is a data structure that stores key value pairs. It uses a hash function to convert keys into unique indexes in an array. This allows for very fast data retrieval, insertion, and deletion, typically in constant time. Hash tables are useful because they make it easy to look up data quickly using a key like name ID or other.